我将根据您提供的详细数据表，对 HMI 界面二（PSK 详情页）进行深度升级，使其从一个通用的演示界面变为基于真实物理参数的 **数字孪生（Digital Twin）** 面板。

### 1. 物理模型校准 (utils/energySimulation.ts)
我将更新后台模拟算法，使其严格遵循您提供的物理参数：
*   **流量计算公式升级**：不再使用估算值，而是基于 **P = ρ·g·h·Q·η** 公式反推。
    *   基准参数：**20 MW** 对应 **4.86 m³/s** 流量。
    *   这意味着当功率为 10 MW 时，实时流量将精确显示为 2.43 m³/s 左右。
*   **数据结构扩展**：在模拟数据流中增加 `flowRate` (m³/s) 和 `efficiency` (%) 字段。

### 2. 界面二：参数面板重构 (PSKDetailView)
我将改造现有的仪表盘，增加一个专业的 **"Anlagendaten"（设备参数）** 模块，展示您提供的具体规格：

*   **静态参数 (Static Data)**：
    *   **Brutto-Fallhöhe**: 508.41 m
    *   **Turbine**: 2x Pelton (Alstom)
    *   **Nennleistung**: 20.0 MW
*   **动态参数 (Live Data)**：
    *   **Wirkungsgrad (效率)**：根据负载动态波动（满载时显示约 93.1%，部分负载略低）。
    *   **Umgebungstemperatur (环境温度)**：基于您提供的气候数据，显示冬日温度（约 -3.4°C）。
    *   **Real-time Flow**: 显示精确到小数点的实时流量。

### 3. 视觉优化
*   增加 **"Pelton Turbine"** 的示意图标或标签，强调这是高水头电站。
*   布局调整，使其更符合工业 SCADA 系统的严谨风格。

这个改动是“非破坏性”的，它只会让现有的 HMI 界面变得更加专业和真实。您确认后我将立即执行。